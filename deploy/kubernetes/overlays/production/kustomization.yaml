apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dsi-prod

resources:
  - ../../base
  - ../../rollout.yaml
  # Production uses ExternalSecrets (C2) — manifests land there.

images:
  - name: ghcr.io/jwalker618/digital-signal-intelligence/dsi-api
    newTag: latest

commonLabels:
  app.kubernetes.io/environment: production
